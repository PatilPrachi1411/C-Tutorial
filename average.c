#include <stdio.h>

int main()
{
    int n,m,p,avg;
    printf("Enter the number:");
    scanf("%d",&n);
    printf("Enter the number:");
    scanf("%d",&m);
    printf("Enter the number:");
    scanf("%d",&p);
    avg=(n+m+p)/3;
    printf("%d",avg);
    return 0;

}